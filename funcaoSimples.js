/*function realizaOperacoes (numero1, numero2) {
    const soma = numero1 + numero2; 
    const subtracao = numero1 - numero2;
    const multiplicacao = numero1 * numero2; 
    const divisao = numero1 / numero2;

    return {
        soma: soma,
        subtracao: subtracao,
        multiplicacao: multiplicacao, 
        divisao: divisao,
    };
};

const resultado = realizaOperacoes(80, 50);

console.log("Soma: " + resultado.soma);
console.log("Subtracao: " + resultado.subtracao); 
console.log("Multiplicacao: " + resultado.multiplicacao);
console.log("Divisao: " + resultado.divisao);*/

function mensagem () {
    console.log("Olá, eu sou uma função")
};

console.log(mensagem())